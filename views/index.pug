extends layout.pug 

block content

  .carousel
    for image, index in carouselImages
      .carousel-image
        img(src=image)
    .carousel-arrow.carousel-arrow-left
      a(href='')='<'
    .carousel-arrow.carousel-arrow-right
      a(href='')='>'
    .carousel-dots
      for image in carouselImages
        a.carousel-dot(href="")
  .section-1
    .container
      h1 Content
  .section-2
    .container
      h1.main-header Featured News
      .upcoming-events-header 
        button UPCOMING EVENTS
      .upcoming-events
        mixin event(imageUrl, title)
          .event-description
            img.event-description-image(src=imageUrl)
            .event-description-title= title
        +event("/event1.png", "IHO WORKTRADE INFO SESSION – Q4!")
        +event("/event1.png", "COCAP 2016 – RADICAL REDISTRIBUTION OF WEALT")
        +event("/event1.png", "THE SWEETWATER SESSIONS, VOL. 5 “BLACK IS BLUE”")
        +event("/event1.png", "CHRISTEN LIEN CONCERT – ELPIS IS COMING")
        //- for event in events
        //-   .event-description
        //-     img.event-description-image(src=event.imageUrl)
        //-     .event-description-title= event.title
      
  .section-4
    .container
      h1 Content
  .section-5
    iframe(width="560" height="315" src="https://www.youtube.com/embed/pmTm77JKPho" frameborder="0" allowfullscreen)
  .section-2
    .container
      h3 Column 1
      h3 Column 2
  .section-3
    .flexbox-container
      h3 Column 1
      h3 Column 2
      h3 Column 3
      h3 Column 4
  .section-2
    .container
      h3 Column 1
      h3 Column 2
      
        
